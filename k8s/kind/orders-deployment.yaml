apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-api-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orders-api
  template:
    metadata:
      labels:
        app: orders-api
    spec:
      containers:
      - name: web
        image: nginx